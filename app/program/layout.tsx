import { ProgramClient } from '../components/pages/ProgramClient'

export default async function ProgramPage({ children }: { children: React.ReactNode }) {
  return <ProgramClient>{children}</ProgramClient>
}
